import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Unternehmens-Rankings Software Sales',
  description: 'Die bestbewerteten Software Sales Organisationen im DACH-Raum – basierend auf echten Bewertungen von Sales-Profis.',
};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
